<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <!-- The above 3 meta tags *must* come first in the head; any other head content must come *after* these tags -->
    <title>Venerable Bede - School Dashboard</title>

    <!-- Bootstrap -->
    <link href="../static/css/yeti-bootstrap.min.css" rel="stylesheet">
    <link href="../static/css/colors.css" rel="stylesheet">

    <!-- HTML5 shim and Respond.js for IE8 support of HTML5 elements and media queries -->
    <!-- WARNING: Respond.js doesn't work if you view the page via file:// -->
    <!--[if lt IE 9]>
    <script src="https://oss.maxcdn.com/html5shiv/3.7.2/html5shiv.min.js"></script>
    <script src="https://oss.maxcdn.com/respond/1.4.2/respond.min.js"></script>
    <![endif]-->

    <script src="../static/js/main.js"></script>
</head>
<body>

<nav class="navbar navbar-default" role="navigation" style="border-radius: 0px;">
    <div class="container-fluid">
        <div class="col-sm-1"></div>
        <div class="col-sm-8">
            <div class="navbar-header">
                Venerable Bede - School Dashboard
            </div>
            <div class="collapse navbar-collapse">
                <ul class="nav navbar-nav navbar-right">
                    <li class="dropdown">
                        <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-expanded="false">Menu <span class="caret"></span></a>
                        <ul class="dropdown-menu" role="menu">
                            {{range .Menu}}
                                {{if .Sep}}<li role="seperator" class="divider"></li>{{else}}
                                <li><a href="{{.URL}}">{{.Name}}</a></li>{{end}}
                            {{end}}
                        </ul>
                    </li>
                    <li><a href="#">Subjects</a></li>
                </ul>
            </div>
        </div>
        <div class="col-sm-2">
            <form class="navbar-form navbar-left form-horizontal" action="../students/" role="search">
                <input type="text" name="date" value="{{.Date}}" style="display: none;">
                <input type="text" name="resultset" value="{{.Resultset}}" style="display: none;">
                <input type="text" name="exams" value="{{.Exams}}" style="display: none;">
                <div class="form-group col-sm-12">
                    <input type="text" name="search" class="form-control input-sm" placeholder="Search students">
                </div>
                <input type="Submit" style="position: absolute; top: -1000px">
            </form>
        </div>
        <div class="col-sm-1"></div>
    </div>
</nav>
